Mono text field with pixel-caps label; focus shows an inset pixel shadow, errors turn the border red.

```jsx
<Input label="Email" placeholder="you@example.com" hint="Newsletter, once a month." />
<Input label="Handle" prefix="@" error="Already taken" />
```
