Multi-pick dropdown; selections appear as removable Tag chips inside the trigger, menu rows use pixel checkboxes.

```jsx
<MultiSelect label="Topics" options={["AI tools","Claude Code","Design","Local models"]} value={topics} onChange={setTopics} />
```
