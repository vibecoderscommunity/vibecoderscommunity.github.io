Single-pick dropdown; menu is a hard-shadowed ink-bordered panel, selection marked with a red ▮.

```jsx
<Select label="City" options={["Tokyo","Singapore"]} value={city} onChange={setCity} />
```

Options can be strings or `{value,label}`.
